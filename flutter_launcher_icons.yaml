# Generates the Windows app icon (taskbar / window / EXE) from the CRMS logo.
# Run after changing the logo:  dart run flutter_launcher_icons
flutter_launcher_icons:
  windows:
    generate: true
    image_path: "assets/images/crms_logo.png"
    icon_size: 256
